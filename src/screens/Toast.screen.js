import React from 'react';
import { connect } from 'react-redux';

const ToastScreen = ({...props}) => {

}

const mapStateToProps = state => {
  return {

  }
}

export default connect(mapStateToProps, null)(ToastScreen);